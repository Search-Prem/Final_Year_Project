{
  "name": "pell-rsa-cloud",
  "version": "1.0.0",
  "private": true,
  "description": "Next Gen Cloud Security Using Pell-Based RSA with Homomorphic Encryption",
  "scripts": {
    "dev:server": "cd server && node index.js",
    "dev:client": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "vercel-build": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^9.2.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
