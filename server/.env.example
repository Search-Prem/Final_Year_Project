# MongoDB Connection String
# Use localhost for local development (default)
# Use mongodb+srv://... for MongoDB Atlas (Cloud)
MONGO_URI=mongodb://localhost:27017/pell_rsa

# Simple JWT Secret for Token Signing
JWT_SECRET=your_super_secret_key_change_this_for_production

# Server Port
PORT=5000
